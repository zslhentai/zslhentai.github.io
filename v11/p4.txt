m()<.5?-1:1)*(2.8+Math.random()*3.8),hitScale:.78,elite:true}))}
function spawnBar(){let st=stageData(),gap=Math.max(92,Math.min(150,W*.34*st.barGap)),gx=18+Math.random()*Math.max(1,W-gap-36),h=24+Math.random()*14,v=(365+Math.random()*65)*st.speed;if(gx>28)blocks.push(makeBlock({type:'bar',x:0,y:-h,w:gx,h,vy:v,hitScale:.94}));let rx=gx+gap;if(W-rx>28)blocks.push(makeBlock({type:'bar',x:rx,y:-h,w:W-rx,h,vy:v,hitScale:.94}))}
function spawnRectAt(cx,yOff=0,scale=1){let st=stageData(),m=st.speed,w=(42+Math.random()*36)*scale,h=(20+Math.random()*16)*scale,x=Math.max(8,Math.min(W-w-8,cx-w/2));blocks.push(makeBlock({type:'rect',x,y:-h-12-yOff,w,h,vy:(360+Math.random()*70)*m,hitScale:.94}))}
function spawnStarAt(cx,yOff=0,dir=0){let st=stageData(),m=st.speed,r=22+Math.random()*8,x=Math.max(r+6,Math.min(W-r-6,cx));blocks.push(makeBlock({type:'star',x,y:-r-18-yOff,radius:r,w:r*2,h:r*2,vy:(390+Math.random()*90)*m,vx:dir*(20+Math.random()*26),spin:(Math.random()<.5?-1:1)*(3+Math.random()*3.2),hitScale:.76,elite:true}))}
function spawnCircleAt(cx,yOff=0,vx=0){let st=stageData(),m=st.speed,r=19+Math.random()*10,x=Math.max(r+6,Math.min(W-r-6,cx));blocks.push(makeBlock({type:'circle',x,y:-r-15-yOff,radius:r,w:r*2,h:r*2,vy:(355+Math.random()*80)*m,vx:vx,hitScale:.92}))}
function spawnHexAt(cx,yOff=0){let st=stageData(),m=st.speed,r=21+Math.random()*10,x=Math.max(r+6,Math.min(W-r-6,cx));blocks.push(makeBlock({type:'hex',x,y:-r-18-yOff,radius:r,w:r*2,h:r*2,vy:(380+Math.random()*80)*m,spin:(Math.random()<.5?-1:1)*(1.7+Math.random()*2.4),hitScale:.82}))}
function patternTwin(){let lane=Math.random()<.5?.38:.62;spawnRectAt(W*(lane-.23),0,1.05);spawnHexAt(W*(lane+.23),18)}
function patternGate(){let gap=Math.max(116,Math.min(154,W*(stage>=4?.29:.33))),cx=W*(.28+Math.random()*.44),left=Math.max(18,cx-gap/2),right=Math.min(W-18,cx+gap/2),h=28,v=(390+Math.random()*55)*stageData().speed;if(left>28)blocks.push(makeBlock({type:'bar',x:0,y:-h,w:left,h,vy:v,hitScale:.93}));if(W-right>28)blocks.push(makeBlock({type:'bar',x:right,y:-h,w:W-right,h,vy:v,hitScale:.93}))}
function patternStarPair(){let gap=W*(stage>=4?.34:.42),cx=W*(.36+Math.random()*.28);spawnStarAt(cx-gap/2,0,1);spawnStarAt(cx+gap/2,26,-1)}
function patternCross(){let r=22,spd=stage>=4?96:78;spawnCircleAt(r+18,0,spd);spawnCircleAt(W-r-18,22,-spd)}
function patternStagger(){let side=Math.random()<.5?-1:1,ax=W*(side<0?.28:.72),bx=W*(side<0?.68:.32);spawnStarAt(ax,0,side);spawnRectAt(bx,54,1.08);if(stage>=3)spawnHexAt(W*.5,108)}
function spawnPattern(){let pool=stage===1?[patternTwin]:stage===2?[patternTwin,patternGate,patternStarPair]:stage===3?[patternTwin,patternGate,patternStarPair,patternCross,patternStagger]:[patternGate,patternStarPair,patternCross,patternStagger,patternTwin],fn=pool[(Math.random()*pool.length)|0];fn();patternCount++;if(patternCount%3===0&&stage>=3)texts.push({x:W/2,y:H*.19,t:'PATTERN ×'+patternCount,life:.68,max:.68,big:false})}
function spawnNormal(){let st=stageData(),r=Math.random();if(Math.random()<st.pattern){spawnPattern();return}if(stage>=2&&r<st.star)spawnStar();else if(r<.24)spawnCircle();else if(r<.46)spawnHex();else if(r<.80)spawnRect();else spawnBar()}
function transition(state){machine.state=state;machine.t=0;machine.phase=0}
function startEvent(kind){for(const b of blocks)release('block',b);blocks.length=0;warnings.length=0;machine.type=kind;machine.total=0;machine.rewarded=false;machine.data={};eventMood=0;recoverT=0;spawnT=999;if(kind===EVENT.METEOR){machine.max=6.2;machine.data.dir=Math.random()<.5?1:-1;machine.data.seed=Math.random()*Math.PI*2;warnings.push({type:'diag',life:1.12,max:1.12,dir:machine.data.dir,curveSeed:machine.data.seed});announce('WARNING','POLYGON STORM · 跟住安全通道')}else{machine.max=5.8;machine.data.firstSide=Math.random()<.5?'left':'right';warnings.push({type:'side',side:machine.data.firstSide,band:'low',life:1.0,max:1.0});announce('WARNING','SIDE BARRAGE · 看左右充能')}transition(STATE.WARNING);sfx('warning');if(navigator.vibrate)try{navigator.vibrate(24)}catch(e){}}
function rewardEvent(label){if(machine.rewarded)return;machine.rewarded=true;let add=45;bonus+=add;eventClears++;eventStreak++;if(machine.type===EVENT.METEOR)meteorClears++;if(machine.type===EVENT.SIDE)sideClears++;texts.push({x:p.x,y:p.y-30,t:label+' +'+add,life:1.05,max:1.05,big:true});if(eventStreak>=2)texts.push({x:p.x,y:p.y-54,t:'EVENT STREAK ×'+eventStreak,life:1.1,max:1.1,big:true});burst(p.x,p.y,22);addRing(p.x,p.y,p.r+8,.55,1.25);shake(.14,5);sfx('clear');if(