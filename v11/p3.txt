l.style.display=DEBUG?'block':'none';
function resize(){W=innerWidth;H=innerHeight;DPR=Math.min(devicePixelRatio||1,2);c.width=W*DPR;c.height=H*DPR;c.style.width=W+'px';c.style.height=H+'px';ctx.setTransform(DPR,0,0,DPR,0,0);p.minY=Math.max(82,70+(parseFloat(getComputedStyle(document.documentElement).fontSize)||16));if(!p.x){p.x=p.targetX=W/2;p.y=p.targetY=H*.78}stars=[];for(let i=0,n=Math.max(44,Math.floor(W*H/10000));i<n;i++)stars.push({x:Math.random()*W,y:Math.random()*H,r:.3+Math.random()*1.5,a:.1+Math.random()*.55})}addEventListener('resize',resize);
function clampPlayer(){p.targetX=Math.max(p.r+4,Math.min(W-p.r-4,p.targetX));p.targetY=Math.max(p.minY+p.r,Math.min(H-p.r-8,p.targetY))}
function acquire(pool,obj){let o=pools[pool].pop()||{};for(const k in o)delete o[k];return Object.assign(o,obj)}
function release(pool,o){if(pools[pool].length<220)pools[pool].push(o)}
function clearObjects(){for(const b of blocks)release('block',b);blocks.length=0;for(const q of particles)release('particle',q);particles.length=0;texts.length=0;warnings.length=0}
function makeBlock(o){return acquire('block',Object.assign({type:'rect',x:0,y:-60,w:60,h:28,vx:0,vy:0,radius:0,angle:0,spin:0,grazed:false,entered:false,passed:false,minGap:9999,event:false,trail:false,hitScale:1},o))}
function resetMachine(){machine.state=STATE.NORMAL;machine.type=EVENT.NONE;machine.phase=0;machine.t=machine.total=machine.max=0;machine.data=null;machine.rewarded=false}
function reset(){started=true;dead=false;paused=false;last=performance.now();time=score=bonus=combo=comboTimer=grazeCount=maxCombo=flash=shakeT=shakeP=eventClears=meteorClears=sideClears=eventStreak=0;stage=1;stageFlash=0;patternCount=0;syncStageUI();spawnT=.16;eventCooldown=stageData().eventMin+1.1;eventMood=0;recoverT=0;nearDanger=0;quality=1;lowFpsT=0;clearObjects();rings.length=0;resetMachine();p.x=p.targetX=W/2;p.y=p.targetY=H*.78;scoreEl.textContent='0';comboHud.style.opacity='0';center.style.display='none';pauseBtn.style.display='flex';pauseBtn.textContent='Ⅱ';wrap.style.transform='translate3d(0,0,0)';announce('STAGE 1','READ THE FIELD · PATTERN LANGUAGE')}
function setPaused(v){if(!started||dead)return;paused=v;dragging=false;pauseBtn.textContent=v?'▶':'Ⅱ';if(v){center.style.display='block';center.innerHTML='<div id="title">已暂停</div><div id="text">分数 '+score+'<br>'+machine.state+(machine.type?' · '+machine.type:'')+'</div><div id="hint">点屏幕继续</div>'}else{center.style.display='none';last=performance.now()}}
function announce(a,b){eventTitle.textContent=a;eventSub.textContent=b||'';eventHud.classList.remove('show');void eventHud.offsetWidth;eventHud.classList.add('show')}
function shake(t,pow){shakeT=Math.max(shakeT,t);shakeP=Math.max(shakeP,pow)}
function endGame(){if(dead)return;dead=true;started=false;pauseBtn.style.display='none';comboHud.style.opacity='0';shake(.22,12);sfx('death');addRing(p.x,p.y,p.r+8,.5,1.5);if(score>best){best=score;bestEl.textContent=best;try{localStorage.setItem('neonBest',best)}catch(e){}}center.style.display='block';center.innerHTML='<div id="title">撞到了</div><div id="text">本局 <b>'+score+'</b> 分<br>生存 '+(time/1000).toFixed(1)+' 秒<br>擦边 '+grazeCount+' 次 · 最高 COMBO ×'+maxCombo+'<br>事件通过 '+eventClears+' 次 · 连胜 ×'+eventStreak+'<br>流星 '+meteorClears+' · 横扫 '+sideClears+'<br>事件奖励 +'+bonus+'</div><div id="hint">点一下重新开始</div><div id="tip">V1.1 · PATTERN LANGUAGE</div>';if(navigator.vibrate)try{navigator.vibrate([28,28,72])}catch(e){}}
function spawnRect(){let st=stageData(),m=st.speed,w=40+Math.random()*66,h=18+Math.random()*24,x=Math.random()*(W-w);blocks.push(makeBlock({type:'rect',x,y:-h-12,w,h,vy:(355+Math.random()*105)*m,hitScale:.96}))}
function spawnCircle(){let st=stageData(),m=st.speed,r=18+Math.random()*14,x=r+Math.random()*(W-2*r),dir=Math.random()<.5?-1:1;blocks.push(makeBlock({type:'circle',x,y:-r-15,radius:r,w:r*2,h:r*2,vy:(350+Math.random()*100)*m,vx:dir*(52+Math.random()*62)*(0.92+.08*stage),hitScale:.94}))}
function spawnHex(){let st=stageData(),m=st.speed,r=20+Math.random()*15,x=r+Math.random()*(W-2*r);blocks.push(makeBlock({type:'hex',x,y:-r-18,radius:r,w:r*2,h:r*2,vy:(370+Math.random()*110)*m,spin:(Math.random()<.5?-1:1)*(1.5+Math.random()*2.3)*(0.95+.08*stage),hitScale:.84}))}
function spawnStar(){let st=stageData(),m=st.speed,r=20+Math.random()*13,x=r+Math.random()*(W-2*r),drift=(Math.random()<.5?-1:1)*(18+Math.random()*32);blocks.push(makeBlock({type:'star',x,y:-r-18,radius:r,w:r*2,h:r*2,vy:(385+Math.random()*125)*m,vx:drift,spin:(Math.rando